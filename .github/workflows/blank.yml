version: 2
updates:
  - package-ecosystem: "npm" # 使用しているパッケージマネージャーを指定（npmの場合）
    directory: "/"           # package.jsonが存在するディレクトリ
    schedule:
      interval: "weekly"     # 更新頻度。daily, weekly, monthlyが指定可能
